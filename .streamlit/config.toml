[theme]
base            = "light"
primaryColor    = "#0068C9"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F0F2F6"
textColor       = "#262730"

[server]
# Raise the default file-upload limit; not needed for this app but good practice.
maxUploadSize   = 200

[browser]
gatherUsageStats = false
